<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Widget.InputLayout" parent="Widget.MaterialComponents.TextInputLayout.FilledBox">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="errorEnabled">false</item>
        <item name="hintTextAppearance">@style/TextStyle.EditText.Hint</item>
        <item name="android:textColorHint">@color/marigold</item>
        <item name="boxCollapsedPaddingTop">10dp</item>
    </style>

    <style name="TextStyle" parent="TextAppearance.AppCompat"/>

    <style name="TextStyle.EditText"/>

    <style name="TextStyle.EditText.Hint">
        <item name="android:textSize">12sp</item>
        <item name="fontFamily">@font/source_sans_pro_regular</item>
        <item name="android:textColor">@color/brown_grey_50</item>
    </style>

</resources>
